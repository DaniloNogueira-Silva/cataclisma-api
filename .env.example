# Conexão com MongoDB (local ou Atlas)
MONGODB_URI=mongodb://localhost:27017/cataclisma

# Segredo para assinar os tokens JWT — troque em produção!
JWT_SECRET=troque_este_segredo_em_producao

# Porta da API (opcional, padrão 3000)
PORT=3000
